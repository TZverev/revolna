import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

const appRoot = document.getElementById('app-root');

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  appRoot
);